#include <stdlib.h>
#include <stdio.h>

/**
 * main - the function main use printf to print numbers
 * del 1 al 10
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; ++i)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
